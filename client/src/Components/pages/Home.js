import React, { Fragment } from 'react';
import Users from '../users/Users';

const Home = () => {
  return (
    <Fragment>
      {/* Displaying user cards */}
      <Users />
    </Fragment>
  );
};

export default Home;
